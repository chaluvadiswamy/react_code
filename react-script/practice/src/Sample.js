import React ,{useRef}

const Sample =() =>{

    const inputVal = useRef('');

    const handlerChange = () =>{
    useEffect (()=>{
        inputVal.
    },[]) ;
}
    return (
        <div>
        
        <input ref={inputVal} type="text" value={inputVal} onChange={handlerChange}/>
         </div>
    )
}